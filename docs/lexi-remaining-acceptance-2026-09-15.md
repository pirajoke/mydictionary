# Lexi: оставшиеся проверки исходного объёма

Статус: выпуск работает; физическая Telegram-приёмка и человеческая проверка
немецкого редакторского draft ещё не выполнены. Проект не закрыт.

## Подтверждено 2026-09-15

- Опубликованный main и оба OVH-сервиса: `83ecd4c3bc908c9c309619fd0dad7541df5dbf7f`.
- PR #144 слит, CI успешен. Предыдущий полный прогон: 1143 теста,
  3 локальных PostgreSQL-only пропуска; PostgreSQL проверен в CI.
- Схема `0023_miniapp_swipe_sessions`, PostgreSQL healthy, heartbeat ready
  (возраст 5.5 с), один poller, ноль рестартов и новых ошибок.
- Loopback/public health 200. Резервная копия от `2026-09-15T03:39:06Z`
  проверена, таймер active, последний сервис success.
- Флаги без изменений: AI/Voice/Stars/Mini App включены;
  Mirror voice/autodeploy выключены.
- Публичный shell объявляет пять preload и пять eager-баннеров; все пять
  файлов HTTP 200, сигнатуры RIFF/WEBP верны, кеш
  `public, max-age=31536000, immutable`. Shell остаётся `no-store`.
  На OVH существующий общий MIME этих файлов — `application/octet-stream`.
- Telegram default menu — Web App с нужным URL; `/app` присутствует в
  default-командах en/ru; защищённые token-файлы совпадают.
  Персональные command scopes без learner-ID не запрашивались.
- PR #143 OPEN/DRAFT, SHA `ebdfb24d39563af77eb3a9da4f8c4b25ba644077`.
  В изолированной копии повторно прошли генератор `--check` и 7 редакторских
  тестов. 100 примеров, 45 записей с дополнительными переводами,
  64 существительных, 10 глаголов. Генератор/каталог/немецкий словарь после
  базы PR не менялись в опубликованном обновлении баннеров.

Проверки выше — серверные, структурные и агентные; они не доказывают
прохождение сценариев на физическом телефоне или согласование лингвистом.

## Марку: приёмка в Telegram на телефоне, около 10 минут

Использовать отдельный тестовый профиль. Ответы в карточках изменяют его
прогресс и XP; агент не создавал и не изменял learner-данные для этой проверки.

| Проверка | Ожидаемый результат | Результат |
|---|---|---|
| Открыть через `/app`, затем через «Меню» | Mini App открывается в Telegram; пять вкладок работают | НЕ ПРОВЕРЕНО |
| Нажать быстрые «Продолжить», «Повторить», «Слова», «Режим», «Язык» | Открываются соответствующие Mini App views. В текущем утверждённом выпуске «Режим» и «Слова» оба открывают выбор слов | НЕ ПРОВЕРЕНО |
| Выбрать слова/режим при активной карточке | Видна форма выбора, сохранённая очередь не заменяется без явного старта | НЕ ПРОВЕРЕНО |
| Оценить одну карточку; закрыть приложение; открыть и продолжить | Возобновляется оставшаяся очередь и прогресс урока | НЕ ПРОВЕРЕНО |
| Проверить левый/правый свайп и кнопки Again/Know, случайный короткий жест | Действия эквивалентны; короткий жест не оценивает карточку | НЕ ПРОВЕРЕНО |
| Завершить короткий урок; сразу нажать Undo (до 10 минут, без новой сессии) | Последняя карточка возвращается, итоговая награда не дублируется | НЕ ПРОВЕРЕНО |
| Язык, узкий экран, клавиатура, светлая/тёмная тема, Arabic RTL | Текущий язык виден; кнопки доступны; текст/навигация не перекрываются | НЕ ПРОВЕРЕНО |
| Если доступен профиль без due: открыть явное повторение | Пустое повторение не подменяется новыми словами | НЕ ПРОВЕРЕНО |
| Во время урока кратко выключить сеть и повторить неудачный ответ после подключения | Есть восстановление; одна оценка применяется один раз | НЕ ПРОВЕРЕНО |
| На первой и повторной загрузке быстро открыть «Настройки» и остальные вкладки | Записать момент появления картинки и ощущаемую задержку; повторное открытие использует кеш | НЕ ПРОВЕРЕНО |

Минимальный первый ответ Марка: «Телефон/ОС/Telegram версия; открытие через
`/app` и Меню — OK/ошибка; Настройки при первом и повторном открытии —
наблюдаемая задержка; продолжение урока после закрытия — OK/ошибка».
Для полного acceptance требуется результат всех строк; недоступные условия
отметить «не выполнялось». Не записывать в публичный журнал имена, Telegram ID,
сообщения, ответы, initData или платёжные данные.
Численный выигрыш относительно старой версии без одинакового baseline не заявлять.

## Человеческая редактура немецкого draft

Передать владеющему немецким и русским редактору
[лист всех 100 записей](../research/lexi-german-human-review-sheet-2026-09-15.md).
Нужны решения по каждой строке, точные правки, дата и проверенный SHA.
После правок проверить изменённые строки повторно и воспроизводимость генератора.
Агентная вычитка и зелёные тесты не являются человеческим approval.
PR #143 остаётся draft; merge/deploy требуют отдельного разрешения Марка
после лингвистического согласования. Никаких CEFR-сертификатов не заявляется.

## Границы продолжения

Новая клавиатура бота, прямой запуск занятий в чате, «Добавить слово» в нижнем
меню, новый персональный `/start` и переделка итоговых кнопок были рекомендациями.
Их реализация не утверждена и не относится к незавершённым действиям этого выпуска.
Откат опубликованного кода: предыдущий reviewed `bd11c6c`, схема неизменна;
не откатывать learner-базу ради изменения интерфейса.
